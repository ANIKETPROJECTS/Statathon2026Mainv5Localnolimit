The interactive "How Anonymization Works" guide (accessible via the Info section, in GuideSection.tsx) still displays the original v2 algorithm's explanations, formulas, and worked examples. All the actual cryptographic fixes (rotl8 diffusion across all 5 keystream bytes, rawKs4 secret-dependent CBC feedback, full 128-bit salt folding into seedA/seedB, SHA-256-based key fingerprinting) were already implemented in code (anonymize.ts) and documented in newfile.md, but none of this is reflected in the guide UI itself. The guide currently looks and behaves exactly as it did before the hardening pass — same steps, same formulas, same worked numbers.

Update every step of the interactive guide to accurately reflect the actual current implementation in the codebase. Specifically:

1. "Set Up Your Example" (Step 1) — no change needed unless input fields reference outdated constraints.

2. "How Keys Are Made" (Step 2) — update to show the corrected full-128-bit export salt folding into seedA/seedB (two independent 32-bit PRNG states via makeKeystream2), not the old single 32-bit exportSaltWord truncation. Update any live-calculated values shown on screen to be generated by calling the actual current makeCellKsBytesV2 / resolveKeyChainAsync functions, not hardcoded from the old version.

3. "Encrypting" (Step 3) — update the explanation and live calculation of the CBC/keystream mechanism to show:
   - rotl8 spreading the chaining value across all 5 sub-operation keystream bytes (not just the first)
   - rawKs4 (the secret, key-derived raw 5th byte) being mixed into the cbc update alongside the ciphertext character code
   - Make clear in the UI copy that this cbc value cannot be reconstructed from ciphertext alone anymore, since rawKs4 is secret
   Update any diagrams, tables, or step-by-step breakdowns currently showing the old formula (cbc ← ((cbc << 3) ⊕ charCode(encChar)) & 0xFF) to show the corrected one.

4. "Decrypting" (Step 4) — same update as above applied to the decryption trace, so the shown formulas match the corrected decryption logic (rawKs4 reproduction on the decryptor side, rotl8 spread across all 5 bytes).

5. "The Full Journey" (Step 5) — update the full end-to-end summary trace and the "Security Properties" section to match the actual current guarantees. Specifically update or add:
   - The per-cell keystream collision bound should state ~2^32 (not any previous 2^16 or 2^64 claim)
   - The "Order-Sensitive" / "Non-malleable" property descriptions should reflect that per-character diffusion now depends on secret key material, not just on public ciphertext
   - Add or correct any explanation of the key fingerprinting mechanism if it is shown, so it reflects the SHA-256 domain-separated hash rather than raw key-slice truncation
   - Reflect that a per-export HMAC-SHA256 tamper-detection tag and audit log (no key material/plaintext) now exist, if the guide summarizes the full pipeline's security properties

REQUIREMENTS:
- All live/interactive calculations shown in the guide (the numbers that update as the user changes seed/column/value inputs) must call the actual current production functions from the codebase — do not hardcode example outputs that could drift out of sync with the real implementation again.
- Do a full pass through every step, table, formula, and worked example currently rendered in the guide UI and cross-check each one against the current source code (the same functions covered in newfile.md's Corrections A, B, and C) to ensure nothing stale remains, the same way the documentation was cross-checked.
- After updating, do a final self-check: pick one sample value, walk it through the guide's displayed steps by hand, and confirm the guide's shown intermediate values match what the actual encrypt/decrypt functions in the codebase would produce for that same input.
- Run a typecheck after all edits and confirm zero errors before finishing.